{
  "_comment": "Gronk runs on the cheapest/fastest model. Swap the `name` (and add/change `provider`) from the TrueForge UI or by editing this file; it is read live on startup. Any OpenAI-compatible model name works.",
  "name": "openai/gpt-4o-mini",
  "provider": "openai"
}
