# ARCHITECTURE_LAYOUT.md — a planned settlement
- Civic core (zone 3): plaza + fountain + lake center; town hall N fronting plaza;
  banner poles at plaza corners; benches ring; lanterns at path mouths.
- Residential W (zone 4): 8-12 houses on lanes branching off the W path, varied
  roof color/rotation, gardens between (no wall-to-wall), each with a REASON
  (lane frontage + garden + tree neighbor).
- Market row E: stalls + storage barn along the E path, crates/barrels/goods.
- Farm district (zone 5): farmhouses at plot corners (supervision), greenhouse at
  E edge, windmill supply path from ridge col.
- Landmarks: windmill (zone 6, skyline), lighthouse (zone 7, isolated),
  waterfall (zone 2, backdrop motion), plaza fountain (foreground civic).
- Every building: path frontage + prop cluster + vegetation neighbor. No isolated
  placements. House archetype first (excellent, then variants); farmhouse rebuilt
  to match; no permanent gen1/visual-mismatch residents.
